{
  "name": "node-app",
  "version": "1.0.0",
  "description": "Node.js application that receives HTTP POST requests and saves data to a file and increments a count in a Redis database, and receives HTTP GET requests and returns the count from the Redis database",
  "main": "index.js",
  "scripts": {
    "test": "mocha"
  },
  "dependencies": {
    "express": "^4.17.1",
    "redis": "^3.0.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "mocha": "^8.1.3"
  }
}
